import re


def normalize_paint(paint):
    """Returns a normalized paint dict, or None if the entry should be skipped."""
    pass


def normalize_hex(value):
    """Returns a lowercase #rrggbb hex string, or None if not convertible."""
    pass
